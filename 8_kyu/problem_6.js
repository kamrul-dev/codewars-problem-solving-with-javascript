/* 
    You only need one - Beginner
*/

function check(a, x) {
    if (a.includes(x)) {
        return true;
    }
    else {
        return false;
    }
}

/* 
    //Very usefull
    function check(a,x){
        return a.indexOf(x) > -1 ? true : false;
};
*/

/* 
    // Best Practices
    const check = (a,x) => a.includes(x);
*/


/* 
function check(a,x){
  return a.includes(x);
};
*/

